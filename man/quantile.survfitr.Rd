% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc-methods.R
\name{quantile.survfitr}
\alias{quantile.survfitr}
\title{Quantiles of the survival time}
\usage{
\method{quantile}{survfitr}(x, probs = c(0.25, 0.5, 0.75), ...)
}
\arguments{
\item{x}{a \code{survfitr} object.}

\item{probs}{vector of probabilities.}

\item{...}{ignored; kept for compatibility with the generic.}
}
\value{
A named vector of survival times, or a matrix with one row per
group when the fit has strata.
}
\description{
Returns, for each requested probability \code{p}, the first time at which
the estimated survival function falls to \code{1 - p} or below (so
\code{probs = 0.5} is the median survival time). \code{NA} is returned when the
curve does not reach the requested level.
}
\examples{
data(MMC)
fit <- survfitr(Survr(id, time, event) ~ group,
  data = MMC,
  type = "wang-chang"
)
quantile(fit, probs = c(0.25, 0.5, 0.75))
}
\seealso{
\code{\link[=q_search]{q_search()}}, \code{\link[=survfitr]{survfitr()}}
}

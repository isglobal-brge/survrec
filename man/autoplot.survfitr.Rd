% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.survfitr.R
\name{autoplot.survfitr}
\alias{autoplot.survfitr}
\title{Plot survival curves with ggplot2}
\usage{
\method{autoplot}{survfitr}(
  object,
  fun = c("surv", "event", "cumhaz"),
  conf.int = TRUE,
  level = 0.95,
  conf.type = c("log-log", "plain"),
  ...
)
}
\arguments{
\item{object}{an object of class \code{survfitr} (output of \code{\link[=survfitr]{survfitr()}},
\code{\link[=psh_fit]{psh_fit()}}, \code{\link[=wc_fit]{wc_fit()}} or \code{\link[=mlefrailty_fit]{mlefrailty_fit()}}).}

\item{fun}{transformation of the survival curve: \code{"surv"} (default,
the survival function), \code{"event"} (the cumulative probability of an
event, 1 - S) or \code{"cumhaz"} (the cumulative hazard, -log S).}

\item{conf.int}{draw the pointwise confidence band? Default \code{TRUE}
(ignored for estimators without standard errors).}

\item{level}{confidence level of the band.}

\item{conf.type}{transformation used for the band: \code{"log-log"}
(default) or \code{"plain"}.}

\item{...}{ignored; kept for compatibility with the generic.}
}
\value{
A \code{ggplot} object, which can be further styled with the usual
\code{ggplot2} syntax.
}
\description{
Draws the estimated survival function of a \code{survfitr} object as a step
function, one colour per group, with an optional pointwise confidence
band. The cumulative distribution (\code{fun = "event"}) and the cumulative
hazard (\code{fun = "cumhaz"}) transformations are also available.
}
\details{
The confidence band is computed from the standard errors of the fit
when the estimator provides them (Peña-Strawderman-Hollander and
Wang-Chang). With \code{conf.type = "log-log"} (default) the limits are
obtained on the \eqn{\log(-\log S)} scale, so they always stay inside
\eqn{[0, 1]}; \code{"plain"} gives the symmetric limits of the base
\code{\link[=plot.survfitr]{plot.survfitr()}} method.
}
\examples{
data(MMC)
fit <- survfitr(Survr(id, time, event) ~ group,
  data = MMC,
  type = "wang-chang"
)
autoplot(fit)
autoplot(fit, fun = "cumhaz", conf.int = FALSE)

# it is a ggplot, so it can be restyled
autoplot(fit) + ggplot2::labs(title = "Migratory Motor Complex")
}
\seealso{
\code{\link[=plot.survfitr]{plot.survfitr()}} for the base-graphics version,
\code{\link[=plotEstimators]{plotEstimators()}} to compare the three estimators,
\code{\link[=theme_survrec]{theme_survrec()}}
}

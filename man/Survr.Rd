% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Survr.R
\name{Survr}
\alias{Survr}
\alias{is.Survr}
\title{Create a survival recurrent object}
\usage{
Survr(id, time, event)

is.Survr(x)
}
\arguments{
\item{id}{identifier of each subject; the same value for all the
recurrent times of a subject.}

\item{time}{gap times of recurrence. The last time of each subject is
censored.}

\item{event}{status indicator: 1 = recurrence, 0 = censored. Only these
values are accepted.}

\item{x}{any R object.}
}
\value{
An object of class \code{Survr}, implemented as a matrix with
columns \code{id}, \code{time} and \code{event}.

\code{is.Survr()} returns \code{TRUE} if \code{x} inherits from class \code{Survr} and
\code{FALSE} otherwise.
}
\description{
Creates a survival recurrent object, usually used as the response
variable in a model formula.
}
\details{
Each subject must contribute one censored time (its last, incomplete
gap time), so \code{id} must have exactly as many distinct values as there
are zeros in \code{event}. Rows are assumed to be grouped by subject and in
chronological order within subject.
}
\examples{
data(MMC)
x <- Survr(MMC$id, MMC$time, MMC$event)
is.Survr(x)
}
\seealso{
\code{\link[=survfitr]{survfitr()}}, \code{\link[=psh_fit]{psh_fit()}}, \code{\link[=wc_fit]{wc_fit()}}, \code{\link[=mlefrailty_fit]{mlefrailty_fit()}}
}
\keyword{survival}

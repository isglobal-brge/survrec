% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.survfitr.R
\name{plotEstimators}
\alias{plotEstimators}
\title{Compare the three survival estimators on the same data}
\usage{
plotEstimators(x, conf.int = FALSE, level = 0.95, alpha.console = FALSE, ...)
}
\arguments{
\item{x}{a survival recurrent event object, see \code{\link[=Survr]{Survr()}}.}

\item{conf.int}{draw the pointwise confidence bands of the estimators
that provide them? Default \code{FALSE}.}

\item{level}{confidence level of the bands.}

\item{alpha.console}{passed to \code{\link[=mlefrailty_fit]{mlefrailty_fit()}}; default \code{FALSE}.}

\item{...}{additional arguments passed to the three fitting functions.}
}
\value{
A \code{ggplot} object.
}
\description{
Fits the Peña-Strawderman-Hollander, the Wang-Chang and the gamma
frailty MLE estimators to the same sample and draws the three curves
on the same axes. Agreement between the PSH and the frailty estimates
suggests independent inter-occurrence times, while a Wang-Chang curve
separated from the PSH one points to within-subject correlation.
}
\examples{
data(MMC)
plotEstimators(Survr(MMC$id, MMC$time, MMC$event))
}
\seealso{
\code{\link[=autoplot.survfitr]{autoplot.survfitr()}}, \code{\link[=psh_fit]{psh_fit()}}, \code{\link[=wc_fit]{wc_fit()}},
\code{\link[=mlefrailty_fit]{mlefrailty_fit()}}
}

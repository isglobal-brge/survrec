% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc-methods.R
\name{as.data.frame.survfitr}
\alias{as.data.frame.survfitr}
\title{Coerce a survfitr object to a data frame}
\usage{
\method{as.data.frame}{survfitr}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{a \code{survfitr} object.}

\item{row.names, optional}{see \code{\link[=as.data.frame]{as.data.frame()}}; ignored.}

\item{...}{ignored.}
}
\value{
A data frame with columns \code{group} (only when the fit has
strata), \code{time}, \code{n.event}, \code{n.risk}, \code{surv} and \code{std.error} (\code{NA}
for estimators without standard errors).
}
\description{
Returns the estimated curves in long ("tidy") format, one row per
distinct event time (and group), ready for further processing or
plotting.
}
\examples{
data(MMC)
fit <- survfitr(Survr(id, time, event) ~ group,
  data = MMC,
  type = "wang-chang"
)
head(as.data.frame(fit))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survdiffr-methods.R
\name{summary.survdiffr}
\alias{summary.survdiffr}
\alias{print.summary.survdiffr}
\title{Summarize a grouped bootstrap of survival quantiles}
\usage{
\method{summary}{survdiffr}(object, level = 0.95, ...)

\method{print}{summary.survdiffr}(x, digits = max(options()$digits - 4, 3), ...)
}
\arguments{
\item{object}{a grouped result of \code{\link[=survdiffr]{survdiffr()}}.}

\item{level}{confidence level of the percentile intervals.}

\item{...}{ignored; kept for compatibility with the generic.}

\item{x}{a \code{summary.survdiffr} object.}

\item{digits}{number of digits to print.}
}
\value{
A list of class \code{summary.survdiffr} with components \code{groups}
(data frame with the observed quantile and its interval per group)
and \code{contrasts} (data frame with the pairwise differences, intervals
and p-values).
}
\description{
Computes, for each group, the observed quantile and a percentile
bootstrap confidence interval, and for each pair of groups the
difference of quantiles with its percentile interval and a two-sided
bootstrap p-value for the null hypothesis of no difference.
}
\details{
Replicates in which the survival estimate never fell below the
requested level are treated as \code{+Inf} (the quantile exceeds the
observed follow-up); pairs where both replicates are infinite are
dropped from the difference. The p-value is
\eqn{2 \min(P(D \le 0), P(D \ge 0))} over the replicated differences
\eqn{D}.
}
\examples{
data(colon)
fit <- survdiffr(Survr(hc, time, event) ~ as.factor(dukes),
  data = colon, q = 0.5, seed = 1
)
summary(fit)
}
\seealso{
\code{\link[=survdiffr]{survdiffr()}}, \code{\link[boot:boot.ci]{boot::boot.ci()}}
}

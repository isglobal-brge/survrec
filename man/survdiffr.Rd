% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survdiffr.R
\name{survdiffr}
\alias{survdiffr}
\title{Bootstrap survival quantiles for recurrent event data}
\usage{
survdiffr(
  formula,
  data,
  q,
  B = 500,
  boot.F = "WC",
  boot.G = "none",
  seed = NULL,
  ...
)
}
\arguments{
\item{formula}{a formula object with a \code{\link[=Survr]{Survr()}} object as the response
on the left of the \code{~} operator and a grouping term on the right.}

\item{data}{a data frame in which to interpret the variables named in
the formula.}

\item{q}{quantile of the survival function whose time is bootstrapped.}

\item{B}{number of bootstrap samples.}

\item{boot.F}{a character string specifying the bootstrap procedure:
\code{"PSH"} or \code{"WC"} (nonparametric) or \code{"semiparametric"}. The default
is \code{"WC"}. Only the first letters are required, e.g. \code{"P"}, \code{"W"},
\code{"se"}.}

\item{boot.G}{a character string specifying whether the censoring times
are also resampled from their empirical distribution: \code{"none"}
(default) or \code{"empirical"}. Only the first letters are required.}

\item{seed}{optional random seed for the bootstrap resampling (passed
to \code{\link[=set.seed]{set.seed()}}). If \code{NULL} (default) the current state of the random
number generator is used.}

\item{...}{additional arguments passed to the estimator used for the
observed (non-bootstrap) quantile.}
}
\value{
An object of class \code{boot} (or a named list with one per group)
whose \code{t0} is the observed quantile and \code{t} the bootstrap replicates.
A replicate is \code{-1} when the survival estimate does not fall below
\code{q}, and \code{NA} when the resampled data had no events. For the
semiparametric procedure the component \code{alpha} contains the frailty
parameter estimated on each replicate.
}
\description{
Obtains bootstrap replicates of a quantile (e.g. the median) of the
survival time for each group of subjects. Confidence intervals of the
replicates can be computed with \code{\link[boot:boot.ci]{boot::boot.ci()}}.
}
\details{
Three resampling schemes are available through \code{boot.F}: nonparametric
resampling of gap times from the Peña-Strawderman-Hollander (\code{"PSH"})
or the Wang-Chang (\code{"WC"}) estimates of the inter-occurrence time
distribution, and semiparametric resampling under the fitted gamma
frailty model (\code{"semiparametric"}). With \code{boot.G = "empirical"} the
per-subject censoring times are additionally resampled from their
empirical distribution.

All resampling uses R's random number generator, so results are
reproducible with \code{\link[=set.seed]{set.seed()}} or the \code{seed} argument. The re-estimation
of the survival curve on each replicate can run in parallel, see
\code{\link[=survrecThreads]{survrecThreads()}}. Some procedures (notably the semiparametric one,
which refits the frailty model on every replicate) can be slow.
}
\examples{
\dontrun{
data(colon)

# compare the median survival time between the three Dukes stages
fit <- survdiffr(Survr(hc, time, event) ~ as.factor(dukes),
  data = colon, q = 0.5, seed = 1
)
boot::boot.ci(fit$"1", type = c("norm", "basic", "perc"))
boot::boot.ci(fit$"2", type = c("norm", "basic", "perc"))
boot::boot.ci(fit$"3", type = c("norm", "basic", "perc"))

# 75th quantile with percentile confidence intervals
fit <- survdiffr(Survr(hc, time, event) ~ as.factor(dukes),
  data = colon, q = 0.75, seed = 1
)
quantile(fit$"1"$t, c(0.025, 0.975))

# resampling from the PSH estimate instead
fit <- survdiffr(Survr(hc, time, event) ~ as.factor(dukes),
  data = colon, q = 0.5, boot.F = "PSH", seed = 1
)
}
}
\references{
González, J.R. and Peña, E.A. (2003). Bootstrapping median survival
with recurrent event data. \emph{IX Conferencia Española de Biometría}.
}
\seealso{
\code{\link[=survfitr]{survfitr()}}, \code{\link[boot:boot.ci]{boot::boot.ci()}}, \code{\link[=survrecThreads]{survrecThreads()}}
}
\keyword{survival}
